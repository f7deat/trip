export type Payload<T> = {
    data: T;
}

export type AmenitiesListItem = {
    id: string;
    name: string;
}

export type RoomListItem = {
    id: number;
    name: string;
    introduction: string;
}